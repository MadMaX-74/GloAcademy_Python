n = int(input('Введите натуральное число: '))
maximum = n
minimum = n
if n >= 2:
    for i in range(n):
        number = int(input('Введите число: '))
        if number > maximum:
            maximum = number
        elif number < minimum:
            minimum = number
print(maximum)
print(minimum)
