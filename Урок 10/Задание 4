symbol_n = input('введите символ: ')
upper_first = 65
upper_last = 90
lower_first = 97
lower_last = 122
if upper_first <= ord(symbol_n) <= upper_last:
    print(symbol_n.lower())
if lower_first <= ord(symbol_n) <= lower_last:
    print(symbol_n.upper())
else:
    print(symbol_n)
